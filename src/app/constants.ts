export const FEEDBACK_CORRECT_MS = 1200;
// longer, so there's time to read the right answer
export const FEEDBACK_WRONG_MS = 2600;
export const QUESTION_COUNT_OPTIONS = [5, 10, 20] as const;
export const DEFAULT_QUESTION_COUNT = 10;
